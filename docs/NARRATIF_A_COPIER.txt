(Rien à copier pour l'instant.)

Le mécanisme a changé : les images des divagations ne passent plus par le frontmatter
Obsidian. Ignore le contenu que je t'avais donné précédemment pour "fontaine-rotonde.md"
(avec les champs "image"/"imageCredit") — pas besoin de le coller, le fichier reste
inchangé avec juste "title"/"text" comme avant. L'image et son crédit sont maintenant
gérés automatiquement côté code (public/divagations/manifest.json), sans aucune
intervention nécessaire dans Obsidian.
